import 'package:flutter/material.dart';

const sliverAppBarHeight = kToolbarHeight + 20;
const sliverAppBarTitleScale = 1.25;
const sliverAppBarTitlePadding = EdgeInsets.all(16);

const bodyPaddingHorizontal = EdgeInsets.symmetric(horizontal: 12.0);
const bodyPaddingVertical = EdgeInsets.symmetric(vertical: 12.0);
